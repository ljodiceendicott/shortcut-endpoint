{
  "name": "weather-api",
  "version": "1.0.0",
  "description": "This is a frontend endpoint that will be used for a project using Docker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
